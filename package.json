{
  "name": "intent-ledger",
  "version": "1.0.0",
  "description": "Intent Accountability Layer for AI/Agentic Commerce",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "ai-agent",
    "intent-ledger",
    "agentic-commerce",
    "fintech",
    "razorpay-buildathon",
    "safety-layer",
    "policy-engine"
  ],
  "author": "IntentLedger Team",
  "license": "MIT"
}
